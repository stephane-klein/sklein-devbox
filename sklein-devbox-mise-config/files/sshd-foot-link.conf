# Remove stale unix-socket reverse-forward listeners (see the foot-link
# systemd unit on the foot host) left behind by a crashed ssh session, so a
# restart can bind them again.
StreamLocalBindUnlink yes
