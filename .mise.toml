[tasks.build]
run = "podman build -t sklein-devbox ."
description = "Build the sklein-devbox container image"

[tasks.enter]
run = "podman run -it --rm sklein-devbox"
description = "Enter the sklein-devbox container shell"
